# Bloqueios

- 2026-09-04 | agente: Yoda (Review Agent) | bloqueio: `mobileTaskRow.vue:9` usa chave de tradução `todo.done` (não existe); correta é `todoDetails.done` — resíduo da WP05, gera warning de i18n no console | escopo afetado: cosmético/console, não trava nenhum critério de aceite | segue andando: sim, todas as WPs seguem | dono da decisão: Maestro — anexar como débito pequeno pra WP de acabamento (WP11) ou correção pontual quando o path estiver livre | resolução: resolvida na WP11 (chave trocada por Rex)
- 2026-09-04 | agente: Rex (Engineer-Mobile) | bloqueio: a DoD da WP09 pede ícone `maskable` no manifest, mas o único ícone grande (`public/fav_icons/android-chrome-512x512.png`) tem cantos e bordas transparentes (verificado pixel a pixel: 0,0 / 2,256 / 509,509 são rgba 0,0,0,0), então declará-lo como maskable mostraria transparência sob máscara quadrada arredondada; um maskable de verdade exige um PNG novo em `public/fav_icons/`, path fora da minha propriedade (WP09 me dá só public/manifest.json, vue.config.js, src/registerServiceWorker.js e mobileApp.vue) | escopo afetado: só o item "ícone maskable" da WP09; instalação, offline e atualização não dependem disso | segue andando: sim, entreguei o resto e declarei `purpose: "any"` nos ícones atuais | dono da decisão: Maestro — liberar `public/fav_icons/` para eu gerar `maskable-512.png` (fundo cheio + zona segura de 80%), ou passar o item para outra WP | resolução: resolvida (decisão do Maestro, liberado public/fav_icons/maskable-512.png)
- 2026-09-04 | agente: Rex (Engineer-Mobile) | bloqueio: a DoD da WP10 pede `ui.mobileWarning` removida dos 19 arquivos, mas a WP me deu só os 16 idiomas restantes; `en.json` e `pt.json` ficaram de fora e ainda têm a chave, então a skill `checar-i18n` (que usa `en.json` como referência) acusa `ui.mobileWarning` faltando nos 16 — não é chave faltando de verdade, é a referência que ainda a carrega | escopo afetado: só esse item da DoD; nenhum texto do app depende da chave, que não tem referência no código | segue andando: sim, os 16 idiomas estão completos e a seção `mobile` foi traduzida em todos | dono da decisão: Maestro — liberar `en.json` e `pt.json` para eu apagar a linha nos dois, ou passar o item para a WP11 | resolução: resolvida (decisão do Maestro, liberado en.json/pt.json só pra essa linha)

- 2026-09-04 | agente: Rex (Engineer-Mobile) | bloqueio: `.claude/specs/current/002-ui-mobile/spec.md` e `plan.md` nunca foram commitados no git — não existem no disco nem no histórico; só `tasks.md`/`tasks-done.md` sobrevivem | escopo afetado: WP11 não tem a lista "Está pronto quando" original pra conferir; contrato original da feature está perdido | segue andando: sim — Rex usa as DoDs das WP01-10 arquivadas em tasks-done.md como lista de aceite equivalente | dono da decisão: Maestro, decidido — se o Arquiteto for recrutado de novo, considerar reconstituir spec.md/plan.md a partir de tasks-done.md pra registro futuro | resolução: contornado pra WP11, reconstituição do arquivo em si pendente

- 2026-09-04 | agente: Rex (Engineer-Mobile-A) | bloqueio: a causa real do bug 3 do HOTFIX-A ("tocar num dia da faixa não troca o dia") não está em `weekDayStrip.vue`: verificado no portal em 390px que o toque comita `setMobileSelectedDate`, o pill preto muda de dia e a lista exibida passa a ser a daquele dia. O que nunca muda é o rótulo de data da barra superior, que usa `moment()` fixo (computed `todayLabel` em `src/components/mobile/mobileApp.vue`) — o usuário toca em SAB 5 e o cabeçalho segue "Sex, 4 Set", daí a leitura de que nada trocou. O mesmo arquivo encadeia o foco do FAB por `$nextTick`, o que no navegador móvel descarta o gesto e impede o teclado de abrir. `mobileApp.vue` não está na lista de files do HOTFIX-A nem pertence ao HOTFIX-B | escopo afetado: só a realimentação visual do bug 3 e a abertura do teclado no aparelho; o critério de aceite escrito ("troca o dia selecionado e a lista corresponde") já passa | segue andando: sim — bugs 1 e 2 corrigidos e validados nos meus três arquivos | dono da decisão: Maestro — liberar `mobileApp.vue` para duas alterações (cabeçalho mostrar a data selecionada; chamada do foco do FAB síncrona) ou abrir WP própria | resolução: resolvida (Maestro liberou mobileApp.vue para as duas mudanças; só a do rótulo foi necessária — o $nextTick do FAB estava em mobileDayView.focusComposer e já saiu de lá)
- 2026-09-04 | agente: Rex (Engineer-Mobile-A) | bloqueio: risco fora do meu escopo, para o dono de `src/assets/style/mobile.scss` (HOTFIX-B): `.mobile-app-shell` usa `height: 100vh`. Em navegador móvel real a barra do navegador reduz a área visível e o `100vh` empurra o FAB e a barra de abas para fora dela, o que casa com o relato "o botão + não funciona" (o toque acerta a interface do navegador, não o botão) e não reproduz no portal, onde a viewport é fixa | escopo afetado: FAB e barra de abas em aparelho real | segue andando: sim | dono da decisão: Maestro — encaminhar ao dono do mobile.scss a troca por `100dvh` com fallback `100vh` | resolução: pendente
- 2026-09-04 | agente: Finn (Engineer-Mobile-B) | bloqueio: o alarme (bug 5 do HOTFIX-B) só é reagendado quando alguma tarefa é gravada. No desktop `App.vue` chama `refreshTodayNotifications()` ao terminar de montar as listas (`todoListMounted`), mas esse ramo não roda no mobile (`v-if="!isMobile"`), então ao recarregar o app no celular nenhuma notificação pendente é rearmada — verificado em portal real: tarefa com hora futura e alarme ligado, `store.getters.notifications.length` = 1 antes do reload e 0 depois. Dentro dos meus arquivos dá pra pedir a permissão no gesto do sino e agendar na hora da edição (feito), mas o rearme no boot exige `src/App.vue` ou `src/components/mobile/mobileApp.vue`, fora da minha propriedade | escopo afetado: alarme sobrevive à sessão aberta, mas não a um reload/reabertura do app antes do horário | segue andando: sim — os 4 bugs do HOTFIX-B estão corrigidos e validados | dono da decisão: Maestro — liberar `mobileApp.vue` (ou `App.vue`) para chamar `notifications.refreshDayNotifications(this, moment().format("YYYYMMDD"))` depois da carga inicial no mobile, ou abrir WP própria | resolução: resolvida (Maestro liberou mobileApp.vue; `mounted()` carrega a lista de hoje e chama refreshDayNotifications — validado com reload e disparo real às 23:33:10)
- 2026-09-04 | agente: Finn (Engineer-Mobile-B) | bloqueio: dev server em `localhost:8080` (de outro agente) servia bundle desatualizado — edições em `mobile.scss` não apareciam nem com reload sem cache, enquanto o sass compilava sem erro. Contornado subindo servidor próprio em `localhost:8091` (`NODE_OPTIONS=--openssl-legacy-provider npx vue-cli-service serve --port 8091`), onde tudo validou | escopo afetado: só a validação em runtime; nenhum efeito no código entregue | segue andando: sim | dono da decisão: nenhum — aviso pro próximo agente que validar em portal: confira se a regra CSS nova existe em `document.styleSheets` antes de concluir que a correção não funcionou | resolução: contornado
